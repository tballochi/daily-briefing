# Pinned to the majors these were tested against, so a fresh clone installs cleanly
# and a breaking upstream release can't silently change the morning run.
groq>=1.4.0,<2.0.0            # Groq SDK (the LLM brain)
tavily-python>=0.7.0,<0.8.0   # live web search
APScheduler>=3.10.4,<4.0.0    # local daily scheduler (4.x is a rewrite)
python-dotenv>=1.0.1,<2.0.0   # reads .env for local runs
PyYAML>=6.0,<7.0              # reads config.yaml
